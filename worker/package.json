{
  "name": "winapps-submit-app-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Thin Cloudflare Worker relay: verifies Turnstile, then hands new app-listing submissions off to a GitHub Actions workflow via repository_dispatch.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "latest"
  }
}
